# 自挂载 bundle patch：`dsh plugin --profile <name> add <本包绝对路径>` 安装后按此声明自动挂载。
# 以包名引用（插件清单页按包名派生显示名，file:// 路径会原样上屏）。
# config 段是插件的组合基座（base）；用户层 settings.yaml 的 dsh-x-model-tuning: 段按 key 合并覆盖。
- insert:
    - id: dsh-x-model-tuning
      name: '@personal/dsh-x-model-tuning'
      config:
        profiles:
          # deepseek/deepseek-chat:
          #   temperature: 0.6
          #   maxTokens: 8192
